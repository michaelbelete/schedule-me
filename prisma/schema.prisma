// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Company {
  id  Int @default(autoincrement()) @id
  name  String
  users User[]
}

model User {
  id  Int @default(autoincrement()) @id
  fullName String @map("full_name")
  email String  @unique
  password String
  company Company?  @relation(fields: [companyId], references: [id])
  companyId Int?
  events Event[]
}

model Event {
  id  Int @default(autoincrement()) @id
  title String
  location String
  start_time DateTime
  end_time DateTime
  user User @relation(fields: [userId], references: [id])
  userId Int 
  attende Attende @relation(fields: [attendeId], references: [id])
  attendeId Int
  @@map("events")
}

model Attende {
  id  Int @default(autoincrement()) @id
  name String
  email String
  event Event[]
  @@map("attendees")
}